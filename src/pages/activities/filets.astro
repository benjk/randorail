---
import ActivityLayout from '../../layouts/ActivityLayout.astro';

import { getActivityContent } from '../../data/activities';

const activityData = getActivityContent('filets')
---

<ActivityLayout {...activityData} />

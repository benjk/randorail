---
import ActivityLayout2 from '../../layouts/ActivityLayout2.astro';

import { getActivityContent, getActivityFAQ } from '../../data/activities';

const slug = 'randorail';
const activityData = getActivityContent(slug)
const faqData = getActivityFAQ(slug);

console.log(activityData);

---

<ActivityLayout2 {...activityData} faq={faqData} fullSlug={slug}>
</ActivityLayout2>
